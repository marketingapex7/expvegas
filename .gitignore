.next/
node_modules/
out/
playwright-report/
test-results/

.env*.local
data/seo-keywords.json

npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# Regenerated by Next on every dev/build, and it points at a different types
# path for each, so tracking it makes the tree dirty after running the tests.
next-env.d.ts
